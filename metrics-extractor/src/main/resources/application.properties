# -----> Render inyecta la variable de entorno PORT en runtime.
# -----> Si no existe (ej. corriendo local), usa 8080 por defecto.
server.port=${PORT:8080}

# -----> Nombre de la app (aparece en logs de arranque de Spring Boot)
spring.application.name=metrics-api